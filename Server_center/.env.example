# Server Center 全局配置（前缀 SC_）

SC_HOST=0.0.0.0
SC_PORT=8765

# Local↔Server 线缆加密（HTTP 响应 / 模块 WS / 终端桥）；UI /local 不受影响
SC_WIRE_ENCRYPT=true

# 远程终端（网页 cmd，经 WebSocket 桥接 Local Agent）
SC_TERMINAL_ENABLED=true
